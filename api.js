// const app = require('express');
(() => {
 const api = {

 };
 
  if (typeof module !== 'undefined' && typeof module.exports !== 'undefined') {
    module.exports = api;
  } else {
    window.api = api;
  }
})();
